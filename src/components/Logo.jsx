import React from "react";

function Logo({ width }) {
    return (
        <img
        src="logo.png"
        alt="logo"
        width={width}
        >
        
        </img>
    )};

export default Logo;